[Unit]
Description=Start script for Hardware Manager
After=network.target

[Service]
ExecStart=uvicorn hardware_manager.hardware_gui:app --host 0.0.0.0 --port 80
Restart=always

[Install]
WantedBy=multi-user.target
